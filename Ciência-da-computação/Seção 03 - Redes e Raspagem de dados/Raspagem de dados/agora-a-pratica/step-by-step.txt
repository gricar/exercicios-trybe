1) Criado ambiente isolado e ativado-o
python3 -m venv .venv && source .venv/bin/activate

2) instalado a biblioteca requests
python3 -m pip install requests

3) instalado a biblioteca parsel
python3 -m pip install parsel

4) instalado a biblioteca pymongo
python3 -m pip install pymongo